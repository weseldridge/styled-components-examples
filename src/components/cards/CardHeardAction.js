import React, { Component } from 'react';
import styled from "styled-components";

const CardHeardAction = styled.div`
    display: inline-block;
    width: auto;
    float: right;
    color: white;
`;

export default CardHeardAction;