import React, { Component } from 'react';
import styled from "styled-components";

const TwoColumnList = styled.section`
    width: auto;
    overflow: auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
`;

export default TwoColumnList;